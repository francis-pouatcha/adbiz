{
    "realm": "adcom",
    "resource": "adbnsptnr.client",
    "realm-public-key": "${realm-public-key}",
    "auth-server-url": "${auth-server-url}",
    "ssl-required" : "external",
    "expose-token": true,
    "credentials": {
        "secret": "${adbnsptnr.client.credential}"
    }
}

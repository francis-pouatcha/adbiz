{
    "realm": "adcom",
    "resource": "adsales.server",
    "realm-public-key": "${realm-public-key}",
    "auth-server-url": "${auth-server-url}",
    "ssl-required" : "external",
    "expose-token": true,
    "credentials": {
        "secret": "${adsales.server.credential}"
    },
    "use-resource-role-mappings":true
}

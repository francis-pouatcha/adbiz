{
    "realm": "adcom",
    "resource": "adcshdwr.server",
    "realm-public-key": "${realm-public-key}",
    "auth-server-url": "${auth-server-url}",
    "ssl-required" : "external",
    "expose-token": true,
    "credentials": {
        "secret": "${adcshdwr.server.credential}"
    },
    "use-resource-role-mappings":true
}

{
    "realm": "adcom",
    "resource": "adbnsptnr.server",
    "realm-public-key": "${realm-public-key}",
    "auth-server-url": "${auth-server-url}",
    "ssl-required" : "external",
    "expose-token": true,
    "credentials": {
        "secret": "${adbnsptnr.server.credential}"
    },
    "use-resource-role-mappings":true
}

{
    "realm": "adcom",
    "resource": "adacc.server",
    "realm-public-key": "${realm-public-key}",
    "auth-server-url": "${auth-server-url}",
    "ssl-required" : "external",
    "expose-token": true,
    "credentials": {
        "secret": "${adacc.server.credential}"
    }
}

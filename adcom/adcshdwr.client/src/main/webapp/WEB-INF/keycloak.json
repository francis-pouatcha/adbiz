{
    "realm": "adcom",
    "resource": "adcshdwr.client",
    "realm-public-key": "${realm-public-key}",
    "auth-server-url": "${auth-server-url}",
    "ssl-required" : "external",
    "expose-token": true,
    "credentials": {
        "secret": "${adcshdwr.client.credential}"
    }
}
